# Phase 0-1: ingestion + service
requests==2.32.3
qdrant-client==1.12.1
sentence-transformers==3.3.1
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.2
openai==1.57.0

# langchain family — ragas 0.2.6 needs this specific generation; letting pip
# resolve these unpinned pulls a much newer langchain (1.x) that removes
# APIs ragas 0.2.6 imports unconditionally (e.g. langchain_community's old
# vertexai path, langchain_core.exceptions.ContextOverflowError). Pin the
# whole family together or a fresh install will break on import.
langchain==0.3.7
langchain-community==0.3.7
langchain-core==0.3.63
langchain-openai==0.2.9
langchain-text-splitters==0.3.3

# Phase 2: evaluation + tracking
ragas==0.2.6
mlflow==2.18.0
datasets==3.1.0

# Phase 4: tracing
langfuse==2.53.9

# dev
python-dotenv==1.0.1
pytest==8.3.3
